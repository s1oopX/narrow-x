---
import BaseLayout from '../layout/BaseLayout.astro';
import EntryList from '../content/EntryList.astro';
import PageHeader from '../content/PageHeader.astro';
import type { Locale } from '../../config/i18n';
import { getDisplayEntries } from '../../lib/content/entries';
import { useTranslations } from '../../i18n/utils';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const posts = await getDisplayEntries('posts', locale);
const t = useTranslations(locale);
---

<BaseLayout title={t('posts.title')} description={t('posts.description')} locale={locale}>
  <PageHeader title={t('posts.title')} description={t('posts.description')} locale={locale} />
  <EntryList entries={posts} collection="posts" locale={locale} priorityFirst />
</BaseLayout>
